<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <application
        android:name = ".interfaces.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gestion_pizzeria_AC03"
        tools:targetApi="31">
        <activity
            android:name=".interfaces.MainActivity"
            android:exported="true">
<!--            aqui arriba  android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar"-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
<!--        activity para pasar los datos-->
        <activity android:name=".interfaces.secondActivity"/>
    <!--        activity para pasar los datos-->
    <activity android:name=".interfaces.configActivity"/>
    </application>


</manifest>